// Eslint JSON config supports comments:
// https://eslint.org/docs/latest/use/configure/configuration-files#comments-in-configuration-files

{
  "extends": [
    "next/core-web-vitals",

    // Make ESLint and Prettier play nice with each other. See:
    // https://prettier.io/docs/en/install#eslint-and-other-linters
    //
    // Must be placed last in the `extends` list. See:
    // https://github.com/prettier/eslint-config-prettier#installation
    "prettier"
  ]
}
